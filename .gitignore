node_modules/
*.tsbuildinfo
*.tgz

# 构建产物 sourcemap 不入库（体积大且对使用者无用）
*.map

# 注意：lib/ 是【有意纳入版本控制】的构建产物。
# 这样克隆下来即可直接 dsh plugin add，无需先 npm install && npm run build。
# 改动 src/ 后请执行 npm run build，并把 lib/ 一并提交。